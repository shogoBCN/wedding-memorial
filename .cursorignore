# .gitignore already excludes most noise. Only extras here:

# Tracked lockfile — low signal for Composer
package-lock.json

# Tracked gallery (270+ WebP) — paths live in HTML/JS, not image bytes
web/assets/images/
